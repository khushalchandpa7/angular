import { CurrConverterPipe } from './add-curr-pipe';

describe('CurrConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
